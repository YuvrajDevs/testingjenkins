{
  "name": "jenkins-project",
  "version": "1.0.0",
  "description": "A simple Node.js app for a Jenkins pipeline.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Tests passed successfully!\" && exit 0"
  },
  "dependencies": {
    "express": "4.18.2"
  }
}
